{
  "name": "spendwise-root",
  "version": "1.1.0",
  "description": "SpendWise - Student Edition MERN Stack Application",
  "main": "server/index.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node server/index.js",
    "dev": "concurrently \"npm run start --prefix server\" \"npm run dev --prefix client\"",
    "render-postbuild": "npm install --prefix server && npm install --prefix client && npm run build --prefix client"
  },
  "keywords": [
    "mern",
    "finance",
    "student",
    "tracker"
  ],
  "author": "SpendWise Team",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}